import { style as s } from 'typestyle';

export const image = s({
    display: 'block',
    margin: 'auto',
    maxWidth: '61.8%',
});

export const imageTitle = s({
    color: 'gray',
    display: 'block',
    margin: 'auto',
    marginBottom: '15px',
    marginTop: '5px',
    maxWidth: '61.8%',
});
